export enum RabbitMQ {
  ApostadorQueue = 'apostadores',
}

export enum ApostadorMsg {
  CREATE = 'CREATE_APOSTADOR',
  FIND_ALL = 'FIND_APOSTADORES',
  FIND_ONE = 'FIND_APOSTADOR',
  UPDATE = 'UPDATE_APOSTADOR',
  DELETE = 'DELETE_APOSTADOR',
}
